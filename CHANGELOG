Version 2.1
- Added support for the GeoHack and MapCompare tools.

Version 2.0
- Added support for Qwant Maps, which is now the default.
- Added a preferences screen for selecting between map providers.
- Added non-default map providers: Google Maps, Bing Maps, Apple Maps (macOS only).
- Added support for relative search queries when latitude and longitude both set to 0.

Version 1.2
- When there is no "q" (search query) parameter, show a marker at the given location.
- Increased minimum supported Firefox version to 68 (current ESR).

Version 1.1
- Support "q" (search query) parameter.
- Provide a default zoom value when not present in geo URI.

Version 1.0
- Initial release.